export const metadata = {
  title: "Photos | PowerTech Solutions",
  description: "Browse our extensive photo gallery of UPS systems, stabilizers, and power backup solutions.",
};

export default function PhotosLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
